<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark-mode launch window. Identical to the light-mode styles by design:
         the app looks the same either way, so both point at the one NavAlert
         background colour rather than at the system's. -->
    <style name="LaunchTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Black.NoTitleBar">
        <item name="android:windowBackground">@color/navalert_background</item>
    </style>
</resources>
